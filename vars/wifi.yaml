---
wifi:
  wlan0:
    # NOTICE: The double quotes around ssid and psk are mandatory. All the
    # values are passed into wpa_supplicant.conf and certain params require to
    # be enclosed in double quotes.
    - ssid: "\"HoloNet\""
      psk: "\"thepassphrase\""
