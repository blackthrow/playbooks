---
users:
  default:
    system_user: false
    image: alpine:latest
    container:
      user: root
      workdir: /root
      command: /bin/sh
  user:
    shell: /usr/local/bin/dosh
    password: blackthrow1
